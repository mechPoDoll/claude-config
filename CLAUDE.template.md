# CLAUDE.md — 새 프로젝트 초기화 템플릿

> **이 파일은 새 프로젝트 초기화 전용 템플릿입니다.**
> 실제 프로젝트에 적용할 때는 `CLAUDE.md`로 복사한 뒤 플레이스홀더를 채우세요.
> 채워진 후에는 아래 자동 분석 지시문 섹션을 삭제하고 핵심 규칙만 유지하세요.

---

## ⚙ 자동 분석 지시문

> **`{{` 플레이스홀더가 남아 있으면, 대화 시작 시 프로젝트를 자동 스캔하여 채울 것.**
> 채운 후 이 섹션("⚙ 자동 분석 지시문")을 삭제하면 매 세션 토큰을 절약할 수 있다.

1. 루트 디렉터리 스캔 → 설정 파일·소스로 기술 스택·패턴·보안 방식 파악
2. `{{}}` 플레이스홀더를 분석 결과로 교체 (불확실하면 유지 후 질문)
3. 사용자에게 결과를 보여주고 승인 후 저장
4. `.env` **값(시크릿)**은 절대 기록 금지 — 키 이름만 참조

---

## 1. 프로젝트 개요

{{프로젝트에 대한 간단한 설명을 작성하세요.}}

| 항목 | 내용 |
|------|------|
| 기술 스택 | {{예: PHP 8.2, MySQL 8.0, Apache / Node.js 20, PostgreSQL / Python 3.12, Django 등}} |
| 프론트 | {{예: jQuery + Bootstrap, React, Vue, Next.js 등}} |
| 모바일앱 | {{예: Android(Kotlin) / iOS(Swift), React Native, Flutter, 또는 "없음"}} |
| 인프라 | {{예: Docker, AWS, Vercel, 온프레미스 등}} |

### 디렉터리 구조 (실제 프로젝트)
```
/
├── CLAUDE.md              # 프로젝트 루트 규칙 (이 파일)
├── QA/                    # QA 산출물 (보안감사 보고서 등)
├── SKILL/                 # SKILL 명세서 (기능/화면 분석)
└── .env                   # 환경변수 (버전관리·배포 제외)
```

---

## 2. 명령어

### 실행 환경
```bash
# 시작
{{프로젝트 시작 명령어}}

# 중지
{{프로젝트 중지 명령어}}

# 로그 확인
{{로그 확인 명령어}}

# DB 접속
{{DB 접속 명령어}}
```

### 빌드
{{빌드 과정을 작성하세요. 빌드 과정이 없으면 "빌드 과정 없음. 파일 수정 즉시 반영." 등으로 기재}}

### 배포
| 항목 | 내용 |
|------|------|
| 배포 방식 | {{예: Git push / 배포 스크립트 / rsync / CI/CD 등}} |
| 배포 대상 | {{예: 운영 서버 IP, 도메인, 경로}} |
| 배포 제외 | {{예: .env, node_modules, .git, log/ 등}} |

---

## 3. 개발 규칙

### 파일 명명 규칙
{{프로젝트의 파일 명명 규칙을 작성하세요.}}

| 패턴 | 용도 | 예시 |
|------|------|------|
| {{패턴}} | {{용도}} | {{예시}} |

### Include / Import 패턴
```
{{프로젝트에서 반복 사용되는 include/import 패턴을 작성하세요.}}
```

### DB 접근 패턴
{{사용 중인 ORM 또는 쿼리 패턴을 기재하세요.}}

```
{{조회, 목록, 삽입, 수정, 삭제 기본 패턴 예시 코드}}
```

### 테이블/컬럼 명명 규칙

| 항목 | 규칙 | 예시 |
|------|------|------|
| 테이블 | {{명명 규칙}} | {{예시}} |
| PK | {{PK 명명 규칙}} | {{예시}} |
| 등록일 | {{등록일 컬럼명}} | {{예시}} |
| 수정일 | {{수정일 컬럼명}} | {{예시}} |

### 응답 패턴
```
{{알림, 리다이렉트, API 응답 등 프로젝트에서 사용하는 응답 함수/패턴}}
```

---

## 4. 보안 규칙 (필수)

> 글로벌 보안 체크리스트(`~/.claude/CLAUDE.md`)가 기본 적용됩니다.
> 아래는 이 프로젝트에 특화된 보안 규칙만 작성하세요.

### 4-1. SQL Injection 방지
```
{{프로젝트에서 사용하는 안전한 쿼리 패턴 — 금지 패턴도 함께 기재}}
```

### 4-2. XSS 방지
```
{{프로젝트에서 사용하는 출력 이스케이프 패턴}}
```

### 4-3. CSRF 방지
```
{{프로젝트에서 사용하는 CSRF 토큰 처리 패턴}}
```

### 4-4. 인증 & 인가
```
{{세션/토큰 구조, 권한 체크 코드 패턴}}
```

### 4-5. 파일 업로드
```
{{허용 확장자 목록, 업로드 처리 함수/패턴}}
```

### 4-6. 비밀번호
{{해싱 방식을 기재하세요.}}

### 4-7. 시크릿 관리
{{환경변수 로드 방식을 기재하세요.}}

### 4-8. 에러 처리
{{운영 환경 에러 노출 방지 규칙을 기재하세요.}}

---

## 5. 코드 패턴

### 목록 페이지
```
{{프로젝트의 목록 페이지 표준 코드 패턴}}
```

### 처리 핸들러 (CREATE/UPDATE/DELETE)
```
{{프로젝트의 CRUD 처리 핸들러 표준 코드 패턴}}
```

---

## 6. QA 핵심 규칙

- QA 파일은 `QA/` 디렉터리에 생성. 작업 시작 시 `QA/index.md` 먼저 생성
- 파일명: `qa_YYYYMMDD_v#.md` / 주제별: `qa_YYYYMMDD_v#_주제.md`
- 보고서 추가·조치 완료 시 `QA/index.md` 갱신 필수
- 상세 절차는 `/qa-audit` 커맨드 참조

## 7. SKILL 핵심 규칙

- SKILL 파일은 `SKILL/` 디렉터리에 생성. 첫 작업 시 `SKILL/00_OVERVIEW.md` 먼저 생성
- 명세서: `NN_섹션명.md` / 변경요청: `CHANGE_MMDD.md`
- 문서 추가·수정 시 `SKILL/index.md` 갱신 필수
- 상세 절차는 `/skill-write` 커맨드 참조

---

## 기여 가이드
- Git 사용 시: PR 제목은 `feat`, `fix`, `docs` 등으로 시작
- 배포 스크립트 사용 시: 수정 파일 목록과 변경 사유를 QA 보고서 또는 커밋 로그에 기록
- 코드 스타일: 네이밍 컨벤션 및 Lint 준수
